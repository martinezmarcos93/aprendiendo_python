# Handoff — 26/09/2026

## Qué se hizo

**Integración y gobernanza**
- ADR-003 a ADR-014 en estado *Propuesta* y regla de gobernanza en [`docs/decisions/README.md`](../decisions/README.md):
  una ADR Propuesta no autoriza implementación.
- `CLAUDE.md` del proyecto: reglas para asistentes y la protección de `backup/*`.
- `backup/main-2026-09-26` (en `7c9c04c`, local y en `origin`) es permanente: nunca se mergea, mueve ni borra. Un hook
  `reference-transaction` en `~/.config/git-hooks/tortuth` (vía `core.hooksPath`) lo hace cumplir en esta compu. En GitHub
  **no tiene protección** (falta un ruleset: lo decide Marcos).

**Producto** (todo en `main`, con tests, validador en 0 errores y entrada en `CHANGELOG.md`)
| Cambio | Qué hace |
|---|---|
| Laberintos (lecciones 13–15 de la tortuga) | se comprueban por reglas del mundo: no tocar paredes y llegar a la salida; vale cualquier ruta |
| Color por nivel | el cuerpo de la tortuga cambia con el nivel de XP; el lápiz arranca siempre verde |
| Cabeceras de seguridad y CSP | `script-src 'self'` sin inline; nosniff, no-referrer, DENY, Permissions-Policy |
| Páginas de error | 400/403/404/500 propias; el 500 muestra un código que queda en el log con la traza |
| Exportar / importar | el progreso a un archivo y de vuelta como perfil nuevo, con validación estricta |
| Pantalla de retorno | "¡Hola de nuevo! Ayer ganaste… Hoy te espera… ▶ Continuar" |
| Ayuda | ❓ Ayuda con 10 preguntas (`contenido/ayuda.json`) |
| Cierre de lección | qué aprendí → qué practiqué → qué gané → qué sigue |

Tests: de 335 a 390 (2 se saltean). Validador: 0 errores en los 4 cursos (los 5 avisos de *Primeros pasos* ya existían).

## Estado actual
- `main` = `origin/main`, árbol limpio. Sin ramas de trabajo abiertas.
- Fase 1 del [roadmap maestro](../experimental/ROADMAP_MAESTRO.md): hecho todo lo que no depende de una ADR Propuesta.

## Próxima sesión — lo que necesita una decisión de Marcos
1. **Fase 0 / Puerta 1:** probar la versión actual con 5–10 chicos. Sin eso no se abre la Fase 2 (Tortuaria).
2. **Aceptar o no** ADR-004 (diagnóstico) y ADR-005 (intereses locales): son lo que falta de la Fase 1.
3. **ADR-009** (`dado()` con semilla): necesaria para un Tortuaria con azar.
4. **GitHub:** ruleset que proteja `backup/*` (borrado, force push, actualizaciones).
5. **Herramientas opcionales:** instalar Playwright (pasar antes por dependency-auditor) para correr
   `jugar_cursos.py`, `revisar_contraste.py` y `revisar_responsive.py`, que son criterios de la Fase 1.

## Notas técnicas
- La CSP no permite `blob:` en `connect-src` ni workers: el confeti corre sin Worker. La descarga del progreso usa un enlace
  `blob:` con `download` (navegación, no `connect-src`).
- En Chrome automatizado con la pestaña oculta, las animaciones de la tortuga (`requestAnimationFrame`) se pausan hasta que
  la pestaña se ve: no es un bug de la app.
- El disco del proyecto es NTFS: los hooks en `.git/hooks` no son ejecutables; por eso el hook vive fuera del repo.
